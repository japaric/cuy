[package]
name = "whoarchi"
edition = "2024"

[lib]
doctest = false

[dependencies]
object = { version = "0.39.1", default-features = false, features = ["build_core", "elf"] }
temp-dir = "0.2.0"

[dev-dependencies]
pretty_assertions = { version = "1.4.1" }
