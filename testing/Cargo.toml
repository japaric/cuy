[package]
edition = "2024"
name = "testing"
publish = false

[lib]
doctest = false

[dependencies]
running-wheel = "0.1.0"

[workspace]
# this should not be cross compiled so put it ins own workspace
