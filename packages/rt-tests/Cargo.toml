[package]
name = "rt-tests"
edition = "2024"
publish = false

[dependencies]
qemu-virt.path = "../qemu-virt"
regs.path = "../regs"
rt.path = "../rt"
sh.path = "../sh"

[features]
codecov = ["sh/codecov"]
